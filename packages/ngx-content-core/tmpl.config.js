export default function (tmplPath, config) {
  config.description = "tools for managing the content in Angular.";
  config.keywords = ["angular", "cms", "content management system", "ngx"];
  return config;
}
