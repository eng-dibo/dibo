// merge this values with tmpl.values.js in workspace's root and packages' root
export default function (tmplPath, config) {
  config.description = "javascript utils";
  config.keywords = ["javascript", "js"];
  return config;
}
